services:
  - type: web
    name: easyride
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DB_PATH
        value: /data/database.sqlite
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
